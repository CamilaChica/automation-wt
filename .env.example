# Authentication
WT_AUTH_ENV=development
WT_AUTH_SECRET=replace-with-a-long-random-secret

# Outlook shared mailboxes
SALES_EMAIL_USERNAME=sales@wingedtycoons.com
SALES_EMAIL_PASSWORD=
PURCHASING_EMAIL_USERNAME=purchasing@wingedtycoons.com
PURCHASING_EMAIL_PASSWORD=

# AI provider credentials (add keys when you have them)
# Keep secrets out of source control; use environment variables or GitHub Secrets.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# Frontend build (set to the deployed API URL when frontend is hosted separately)
VITE_API_BASE_URL=/api
FRONTEND_ORIGIN=http://localhost:3000
